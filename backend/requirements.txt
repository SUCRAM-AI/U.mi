# Flask API - Dependências Essenciais
# API REST para integração com frontend React Native
#
# Para instalar: pip install -r requirements.txt

# Framework web
flask>=3.1.0,<4.0.0
flask-cors>=5.0.0,<6.0.0

# Utilitários HTTP e requisições (para API externa e CifraClub)
requests>=2.32.0,<3.0.0

# Gerenciamento de variáveis de ambiente (.env)
python-dotenv>=1.0.0,<2.0.0

# Werkzeug (vem com Flask, mas especificando para compatibilidade)
werkzeug>=3.1.0,<4.0.0
